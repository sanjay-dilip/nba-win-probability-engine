# Legacy manual workflow.
# Scheduled Finals refresh is handled by Google Cloud Run Job + Cloud Scheduler.

name: Finals data refresh

on:
  workflow_dispatch:

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Scheduled Finals refresh is handled by Google Cloud Run and Cloud Scheduler."